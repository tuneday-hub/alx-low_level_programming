#include "main.h"

/**
 * print_alphabet - prints alphabets in lowercase
 *
 * author: tuneday
 * Return: nothing, just void
 */
void print_alphabet(void)
{
	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
}
